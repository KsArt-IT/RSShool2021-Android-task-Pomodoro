<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Pomodoro" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_green_300</item>
        <item name="colorPrimaryDark">@color/light_green_dark_300</item>
        <item name="colorPrimaryVariant">@color/light_green_light_300</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/yellow_300</item>
        <item name="colorSecondaryVariant">@color/yellow_light_300</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="toolbarStyle">@style/ToolbarStyle</item>
    </style>
    <!--    <style name="ToolbarStyle" parent="ThemeOverlay.MaterialComponents.Toolbar.Surface">-->
    <style name="ToolbarStyle" parent="Widget.MaterialComponents.Toolbar.Surface">
        <!-- цвет туллбара -->
        <!--        <item name="android:background">?attr/colorSurface</item>-->
        <!-- возвышение над контентом-->
        <item name="android:elevation">0dp</item>
        <item name="android:tint">?attr/colorPrimary</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <!--        <item name="app:navigationIconTint">0dp</item>-->
        <!-- тема для всплывающего меню       -->
        <!--        <item name="popupTheme">@style/Theme.MaterialComponents.DayNight</item>-->
    </style>
</resources>